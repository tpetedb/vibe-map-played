# The OpenCode team

## What I learned

Running the same prompt through two providers made it obvious which part of my setup is actually mine. The rules file, the checks and the review step survived the swap untouched, and the model turned out to be the easiest piece to replace of the whole lot.
